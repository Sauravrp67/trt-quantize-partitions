# Sample recordings

The README embeds the FP32, FP16, INT8 and FP8 Jetson recordings as GitHub-hosted videos. Large local copies are kept under `samples/jetson/` and excluded from Git.

Bring an unannotated input video for inference, or use a camera/stream. Annotated demo recordings are outputs and should not be used as benchmark inputs.
