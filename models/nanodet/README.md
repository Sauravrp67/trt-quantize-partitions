# NanoDet Model Assets

Placeholder for NanoDet export notes, checkpoint placement, ONNX naming conventions, and TensorRT partition constraints.

TODO:

- Document the exact NanoDet variant and checkpoint source.
- Record preprocessing, input resolution, anchor/grid metadata, and decode assumptions.
- Keep checkpoint files under `checkpoints/`; they are gitignored.
