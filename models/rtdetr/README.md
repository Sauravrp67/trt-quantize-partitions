# RT-DETR Model Assets

Placeholder for RT-DETR export notes, checkpoint placement, ONNX naming conventions, and TensorRT partition constraints.

TODO:

- Document the exact upstream RT-DETR implementation and checkpoint source.
- Record preprocessing, image size, class metadata, and postprocessing assumptions.
- Keep checkpoint files under `checkpoints/`; they are gitignored.
